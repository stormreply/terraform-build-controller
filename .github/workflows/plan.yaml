name: Plan

permissions: write-all

on:
  workflow_dispatch:
    inputs:
      account_id:
        type: string
        required: true
        default: 541792499640
      environment:
        type: string
        required: true
        default: prod
      region:
        type: string
        required: true
        default: eu-central-1
      tfvars:
        type: string
        required: false
        default: ""

jobs:
  plan:
    uses: stormreply/terraform-workflow-controller/.github/workflows/plan.yaml@main
    with:
      account_id: ${{ inputs.account_id }}
      environment: ${{ inputs.environment }}
      region: ${{ inputs.region }}
      var_file: ${{ inputs.tfvars }}
    secrets: inherit
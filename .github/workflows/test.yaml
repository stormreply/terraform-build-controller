name: Test

permissions: write-all

on:
  workflow_dispatch:
    inputs:
      account_id:
        type: string
        required: true
        default: 541792499640
      environment:
        type: string
        required: true
        default: prod
      region:
        type: string
        required: true
        default: eu-central-1
      tfvars:
        type: string
        required: false
        default: ""

jobs:
  test:
    uses: stormreply/terraform-workflow-controller/.github/workflows/test.yaml@main
    with:
      account_id: ${{ inputs.account_id }}
      environment: ${{ inputs.environment }}
      region: ${{ inputs.region }}
      tfvars: ${{ inputs.tfvars }}
    secrets: inherit